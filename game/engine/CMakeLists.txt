file(GLOB SOURCES "include/*.hpp" "src/*.cpp")

ADD_LIBRARY(LIB_ENGINE STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(LIB_ENGINE glfw g3logger)
TARGET_INCLUDE_DIRECTORIES(LIB_ENGINE PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
