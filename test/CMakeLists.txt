include_directories(${gtest_SOURCE_DIR}/include ${gtest_SOURCE_DIR})
file (GLOB SOURCES "src/*.cpp")
add_executable(runTest ${SOURCES})
target_link_libraries(runTest PRIVATE gtest)
target_link_libraries(runTest PRIVATE gtest_main)
TARGET_INCLUDE_DIRECTORIES(runTest PUBLIC include)
TARGET_INCLUDE_DIRECTORIES(runTest PRIVATE src)
